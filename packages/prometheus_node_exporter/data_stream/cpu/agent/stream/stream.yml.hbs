metricsets: ["collector"]
hosts:
{{#each hosts}}
  - {{this}}
{{/each}}
period: {{period}}
timeout: {{timeout}}
metrics_path: {{metrics_path}}
metrics_filters:
  include:
    - "node_cpu_seconds_total"
    - "node_cpu_guest_seconds_total"
{{#if username}}
username: {{username}}
{{/if}}
{{#if password}}
password: {{password}}
{{/if}}
{{#if tags}}
tags:
{{#each tags as |tag|}}
  - {{tag}}
{{/each}}
{{/if}}
{{#if processors}}
processors:
{{processors}}
{{/if}}
